"""
{{.Description}}
Generated by AstrCode - AstrBot Intelligent Development Assistant
"""

from astrbot.api import *
from astrbot.api.event import filter
import logging

logger = logging.getLogger(__name__)


@register(name="{{.Name}}", description="{{.Description}}", version="{{.Version}}", author="{{.Author}}")
class {{.ClassName}}Plugin(Star):
    def __init__(self, context: Context, config: AstrBotConfig):
        super().__init__(context)
        self.config = config
        logger.info("{{.Name}} plugin initialized")

{{range .Handlers}}
    @{{.Type}}
    async def {{.Name}}(self, event: AstrMessageEvent):
        """{{.Description}}"""
        try:
            # TODO: Implement your logic here
            message = event.message_str
            await event.send(f"Received: {message}")
        except Exception as e:
            logger.error(f"Error in {{.Name}}: {e}")
            await event.send(f"Error occurred: {str(e)}")

{{end}}
